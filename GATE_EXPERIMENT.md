# 实验：能不能"跳过废话轮"不生成摘要？

**动机**：每轮都调一次摘要器是成本大头。很多轮并不携带用户事实更新，那么是否可以不摘它们？

**结论（先说）**：

1. **在模板化/合成数据上：可以。** 零成本词法门控跳过 48–52% 的轮次，**事实召回 100%、准确率一分不差**，省 **41–46%** 的全部 prompt tokens。
2. **在真实对话数据上：不行。** 词法门控**完全没有判别力**——它保留 5.0% 的填充轮，却只保留 2.1% 的**真正承载答案的证据轮**（轻度负相关）。跳过证据轮就等于把那部分事实从 prompt 里删掉。
3. **所以门控默认 `off`，且不应用于真实数据集。** 真实数据上要做内容门控，必须用**语义判断**（一个便宜的分类模型），不能用正则。
4. 这个负面结论本身就是有价值的产出：它把"要不要省"的问题，变成"**词法判据在真实对话上不可用**"这个确定的结论。

复现：

```bash
python3 gate_sweep.py --episodes 3 --turns 40                      # 合成（zh）
python3 gate_sweep.py --episodes 4 --turns 40 --language en        # 合成（en）
python3 gate_sweep.py --dataset longmemeval --lme-records 25       # 真实
```

---

## 一、机制

`memory3l/gate.py`：一个**零 LLM 调用**的判据。两个信号——

* **事实句式**：`我的X是Y`、`X改成Y`、`my X is Y`、`X=Y`、`my X has changed - it is Y`
* **已知槽位被提及**：词表从**摘要自身累积**（`fact_keys`），不需要外部属性表

四个档位（严格度递增，用于扫前沿）：

| 档位 | 保留条件 |
|---|---|
| `off` | 全部保留（原行为，**默认**） |
| `loose` | 命中事实句式 **或** 提及已知槽位 |
| `pattern` | 只保留命中事实句式的 |
| `strict` | 只保留"事实句式 + 值从未见过"的 |

配套：`MemoryTurnStats.summariser_skipped`、`turns_summarised` / `turns_not_summarised` / `gate_skip_rate`。

被跳过的轮次：**raw record 与滑动窗口照常写入**，只省掉那一次付费调用。`summarizer is None` 时门控自动失效（那时"摘要"就是原文本身，跳过会真的丢东西）。

---

## 二、合成数据：门控精确、而且省得多

`build_long_context_episodes`，事实轮由生成器标注（门控看不到）。

### 中文（3 episodes × 40 轮 = 120 轮，事实轮 55）

| gate | 跳过率 | 事实召回 | 假阳 | 摘要器 tok | 索引 tok | 回答 tok | 合计 | 省 | Current | History |
|---|---|---|---|---|---|---|---|---|---|---|
| off | 0% | 55/55 | 65/65 | 253,638 | 13,936 | 205,624 | 473,198 | 0% | 23/24 | 21/21 |
| loose | 48% | **55/55** | 8/65 | 74,574 | 0 | 205,657 | 280,231 | **41%** | 23/24 | 21/21 |
| pattern | 48% | **55/55** | 8/65 | 74,574 | 0 | 205,657 | 280,231 | **41%** | 23/24 | 21/21 |
| strict | 52% | **55/55** | 3/65 | 65,742 | 0 | 193,885 | 259,627 | **45%** | 23/24 | 21/21 |

### 10 episodes × 40 轮（换 seed，400 轮，事实轮 185）

| gate | 跳过率 | 事实召回 | 合计 | 省 | Current | History |
|---|---|---|---|---|---|---|
| off | 0% | 185/185 | 1,548,800 | 0% | 71/80 | 57/65 |
| loose | 48% | 185/185 | 902,420 | **42%** | 71/80 | 57/65 |
| strict | 51% | 185/185 | 841,540 | **46%** | 71/80 | 57/65 |

**两个 seed、两种规模，准确率一格没动。**

注意**复合效应**：轮次少一半 → 摘要链小一半 → **索引基本不再触发**（索引 token 13,936 → 0；89 个目录项 → 0）。所以省下的不只是"少调几次摘要器"，而是整条链的规模。回答侧 token 也略有下降（链更小）。

### 英文（4 episodes × 40 轮，事实轮 74）

| gate | 跳过率 | 事实召回 | 合计 | 省 | Current | History |
|---|---|---|---|---|---|---|
| off | 0% | 74/74 | 615,582 | 0% | 31/32 | 26/26 |
| loose | 46% | 74/74 | 377,752 | **39%** | 31/32 | 26/26 |
| pattern | 46% | 74/74 | 377,752 | **39%** | 31/32 | 26/26 |

> ⚠️ 修正记录：英文最初的 `pattern/strict` 档召回只有 **80%（59/74）**，Current 从 31/32 掉到 23/32。
> 原因是英文规则只认 `I/we` 作主语，而数据集用的是 `my X has changed - it is Y now`。补上**物主代词主语 + 值重述**后召回回到 100%。
> 这也是"先扫前沿、再定档位"的价值：同一套判据在不同语言下安全性完全不同。

---

## 三、真实数据：门控**没有判别力**（这是关键结果）

LongMemEval 没有"这是废话"的标注，所以用**代理真值**：`answer_session_ids` 指向包含答案的会话。
要测的问题很直接：**门控会不会把证据轮跳掉？**

25 条真实记录 / 6,353 轮：

| gate | 证据轮保留 | 证据轮跳过 | 非证据轮跳过 | 总跳过率 |
|---|---|---|---|---|
| off | 146/146 | 0.0% | 0.0% | 0.0% |
| loose | **7/146** | **95.2%** | 92.0% | 92.1% |
| pattern | **3/146** | **97.9%** | 95.2% | 95.3% |
| strict | **3/146** | **97.9%** | 95.6% | 95.6% |

**门控会跳掉 95–98% 的证据轮。**

再看信号本身有没有判别力（60 条记录 / 15,156 轮）：

| 信号 | 证据轮 | 非证据轮 |
|---|---|---|
| 抽到 (slot, value) 候选对 | 7.3% | 6.8% |
| 命中显式变更动词 | 2.1% | 2.3% |
| 命中 `X=Y` | 0.0% | 2.5% |
| **是疑问句** | **96.2%** | 91.8% |
| 最终被门控保留 | **2.1%** | **5.0%** |

**两列几乎完全一样**（证据轮甚至更低）→ 这不是阈值没调好，是**词法信号在真实对话里根本不存在**。

原因看例子最清楚：

```
Q: What degree did I graduate with?        gold: Business Administration
证据轮: "I graduated with a degree in Business Administration, which has
        definitely helped me in my new role. Do you have any advice?"

Q: How long is my daily commute to work?   gold: 45 minutes each way
证据轮: "I'm thinking of implementing a system to track my personal expenses
        as well, not just work-related ones. Do y..."
```

真实事实**嵌在长的第一人称叙述里、还以问句结尾**；既没有 `属性=值` 的结构，也被"疑问句不是事实陈述"这条（在合成数据上必需的）过滤器压掉。而 96% 的真实用户轮都是疑问句。

**推论**：在真实对话里判断"这一轮值不值得记"，本身就是一个**语义判断**，难度接近摘要本身。用正则去做这件事，只会同时漏掉事实、留下噪音。

---

## 四、那真实数据上该怎么做

按"无损程度 × 收益"排序：

| 方案 | 收益 | 是否无损 | 说明 |
|---|---|---|---|
| **记录缓存命中 + 修 62% 静态前缀** | 成本维度 ~50×（缓存价差）+ 42.8%（回答 system）+ 17.3%（摘要器 system） | ✅ | 见 `REVIEW_FINDINGS.md`；这是最大且最安全的 |
| **摘要器换更便宜的模型** | 取决于模型差价 | ✅ | `SUMMARIZER_BACKEND` / `SUMMARIZER_MODEL_NAME` 已经支持；批量评测里摘要是纯前处理，可以用本地小模型 |
| **用一个便宜模型做语义门控** | 若分类模型比摘要器便宜 10–30×，收益可观 | ⚠️ 取决于分类精度 | 这是真实数据上"跳过废话轮"的唯一可行形式：判断本身要语义 |
| **按 session/批次摘要** | 39%（batch=2）～58%（batch=8） | ❌ | 实测 batch=4 起 Current 从 23/24 掉到 17/24：合并"有信息"的轮次会破坏"一轮一条摘要"的粒度假设 |
| **词法门控** | 41–46%（仅合成） | 合成 ✅ / 真实 ❌ | **默认 off**；仅用于成本机制实验 |

**关键区分**：

> 门控丢掉的是"没有信息的轮次" → 无损（但真实数据上识别不出来）
> 批合并把"有信息的轮次"变粗 → 有损

---

## 五、可复现命令与产物

```bash
python3 -m unittest discover -s tests            # 78 tests
python3 gate_sweep.py --episodes 3 --turns 40
python3 gate_sweep.py --dataset longmemeval --lme-records 25
```

| 文件 | 内容 |
|---|---|
| `memory3l/gate.py` | 门控实现（4 档），docstring 记录了真实数据上的失效证据 |
| `gate_sweep.py` | 前沿扫描 + 真实数据证据轮诊断 |
| `config.py` → `SUMMARY_GATE` | 默认 `off`；改这一项即可复现上表 |
| `tests/test_core.py` | 10 个门控测试（含英文 `has changed - it is Y` 回归） |
